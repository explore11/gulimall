<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.song.gulimall.product.dao.ProductAttrValueDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.song.gulimall.product.entity.ProductAttrValueEntity" id="productAttrValueMap">
        <result property="id" column="id"/>
        <result property="spuId" column="spu_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
        <result property="quickShow" column="quick_show"/>
    </resultMap>

    <resultMap id="SpuItemAttrGroupMap" type="com.song.gulimall.product.vo.SpuItemAttrGroupVo">
        <result property="groupName" column="attr_group_name"></result>
        <collection property="attrs" ofType="com.song.gulimall.product.vo.spu.Attr">
            <result property="attrName" column="attr_name"></result>
            <result property="attrValue" column="attr_value"></result>
        </collection>
    </resultMap>

    <select id="getProductGroupBySpuId" resultMap="SpuItemAttrGroupMap">
        SELECT
            pag.attr_group_name ,
            pag.attr_group_id ,
            paar.attr_id,
            pa.attr_name,
            ppav.attr_value
            FROM pms_attr_group  pag
            LEFT JOIN pms_attr_attrgroup_relation paar ON pag.attr_group_id = paar.attr_group_id
            LEFT JOIN pms_attr pa ON paar.attr_id = pa.attr_id
            LEFT JOIN pms_product_attr_value ppav ON pa.attr_id = ppav.attr_id
        WHERE pag.catelog_id = #{catalogId} AND ppav.spu_id =#{spuId}
    </select>


</mapper>
